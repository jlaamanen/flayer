export function saveData(a: number, b: string): Promise<void>;
export function getData(): Promise<Session>;

interface Session {
    a: number;
    b: string;
}

interface Session {
    a: number;
    b: string;
}
